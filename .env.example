# SecAI Radar – local env template (mirrors ctxEco POSTGRES_* pattern)
# Copy to .env and fill in values. Do not commit .env.
# Full guide: docs/CREDENTIALS-SETUP.md

# ==========================================================================
# Database (PostgreSQL) – same layout as ctxEco/.env
# ==========================================================================
# Azure (ctxeco-db): use secairadar_app after running scripts/create-secairadar-db-user.py
POSTGRES_HOST=ctxeco-db.postgres.database.azure.com
POSTGRES_PORT=5432
POSTGRES_USER=secairadar_app
POSTGRES_PASSWORD=<from Key Vault or create-secairadar-db-user.py output>
POSTGRES_DB=secairadar

# Apps read DATABASE_URL; build from above or set directly:
# DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}
DATABASE_URL=postgresql://secairadar_app:<PASSWORD>@ctxeco-db.postgres.database.azure.com:5432/secairadar

# Optional: frontend API base (apps/public-web)
# VITE_API_BASE=https://secai-radar-api.azurewebsites.net/api
