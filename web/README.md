
# SecAI Radar Web (Vite + React)
Pages to wire next:
- /signin (SWA auth)
- /tenant/:id/dashboard  (GET /summary, radar)
- /tenant/:id/controls   (GET /controls; import POST /import)
- /tenant/:id/tools      (GET/POST /tools)
- /tenant/:id/gaps       (GET /gaps, with explainability)
