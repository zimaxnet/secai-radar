[project]
name = "secai-radar-graph-builder"
version = "1.0.0"
description = "Graph Builder worker for GK Explorer"
requires-python = ">=3.11"
dependencies = [
    "psycopg2-binary>=2.9.0",
]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
